<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The MEAN Stack</title>
</head>
<body>
    <h1><%= name %></h1>
    <h2><%= headerTitle %></h2>

    <ul>
        <% for(let i = 0; i < definitions.length; i++) { %>
            <li>
                <strong><%= definitions[i].name %></strong>: <%= definitions[i].description %>
            </li>
        <% } %>
    </ul>
</body>
</html>
